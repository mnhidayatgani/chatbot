{
  "name": "whatsapp-shopping-chatbot",
  "version": "1.0.0",
  "description": "WhatsApp Shopping Chatbot Assistant for selling premium accounts and virtual credit cards",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "test": "jest --coverage",
    "test:unit": "jest --testPathPattern=tests/unit",
    "test:integration": "jest --testPathPattern=tests/integration",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --coverageThreshold='{\"global\":{\"lines\":80}}'",
    "lint": "eslint *.js lib/*.js services/*.js src/**/*.js",
    "lint:fix": "eslint *.js lib/*.js services/*.js src/**/*.js --fix",
    "setup": "npm install && echo '\n✅ Dependencies installed!\n📝 Next: Copy .env.example to .env and configure\n🚀 Run: npm start'",
    "quick": "npm install && npm run lint && npm test",
    "check": "npm run lint && npm test",
    "precommit": "npm run lint && npm test",
    "deploy:check": "npm run lint && npm test && echo '\n✅ All checks passed! Ready to push.'"
  },
  "keywords": [
    "whatsapp",
    "chatbot",
    "shopping",
    "assistant"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@ai-sdk/google": "^2.0.26",
    "ai": "^5.0.86",
    "axios": "^1.13.1",
    "body-parser": "^2.2.0",
    "crypto-js": "^4.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "redis": "^5.9.0",
    "whatsapp-web.js": "^1.34.1",
    "xendit-node": "^7.0.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "eslint": "^9.39.0",
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  }
}
